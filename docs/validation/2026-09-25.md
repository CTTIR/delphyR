# Lokaler Entwicklungsnachweis vom 25.09.2026

R 4.6.1 auf Linux; PostgreSQL 17, getestetes Image
`sha256:67f41722b7a8cbdb868a44a4995c846eddfdc2973bccb291ce937dce88ad5675`.
R-Packageversionen stehen in `renv.lock`. Alle Eingaben sind synthetisch.

## Ausgeführte Kommandos

```sh
DELPHYR_TEST_DB=true Rscript scripts/integration.R
Rscript scripts/demo-e2e.R
Rscript scripts/migration-empty.R
Rscript scripts/check-packages.R
bash scripts/restore-check.sh
```

- Integration: 59 + 6 + 17 + 30 + 38 = **150 Assertions**, keine Fehler/Warnungen.
- Reine Kernverträge: **63 Assertions**, keine Fehler/Warnungen.
- Shiny-Modulverträge: **40 Assertions**, einschließlich fehlgeschlagenem
  Save, schmutzigem Formular, Abgabe mit exaktem Revisionssatz, Feedbackfreigabe
  und Zeitzonendarstellung. Aktueller Package-Testlog ist maßgeblich.
- Zweirundiger Servicepfad: 30 synthetische Personen in zwei Gruppen, zwölf
  zweisprachige Items, 720 bestätigte Antworten, 60 Abgaben, eigene Vorantworten,
  Feedback, Abschlussentscheidungen, privater Export und identische Offlineanalyse.
- Packagechecks: beide **Status OK** mit `--as-cran --no-manual` und deaktivierter
  CRAN-Incomingprüfung. Keine CRAN-Einreichung oder CRAN-Annahme behauptet.
  Datenbanktests werden im normalen Packagecheck bewusst übersprungen und
  durch den separaten expliziten PostgreSQL-Lauf nachgewiesen.

## Browser

Chromium unter Linux: ausgewählte Sprache erhält ungespeicherte Werte;
Savebestätigung erst nach Serviceantwort; ausdrückliche Abgabe mit Receipt.
Responsive Bildschirmprüfung bei 390 Pixeln ohne horizontales Überlaufen.
Zusätzlich echter PostgreSQL-Panelpfad unter `delphyr_runtime`: Einwilligung,
Wert 7, Revision 1, Status `submitted` und genau eine gespeicherte Quittung
unabhängig vom Browser erneut abgefragt. Reproduzierbare Skripte und genaue
Grenzen unter `packages/delphyrApp/inst/qa/`.

Die initialen Screenshots nutzen injizierte Services. Sie werden nicht als
PostgreSQL-, Accessibility-, Cross-Browser- oder Management-End-to-End-Abnahme
umgedeutet. Ein zusätzlicher echter Management-Start wurde in Chromium geprüft;
der vollständige Managementprozess bleibt ein offenes Gate.

## Restore

Finaler Lauf `.checks/restore-20260925T093049Z-4hgoqegd/result.json`: 44 Tabellen,
32 Snapshots, alle sechs Migrationschecksummen, Zeilenfingerprints und Schema
stimmen in einer frisch gestarteten, netzisolierten Instanz überein.
Dump-SHA256: `d4e37589c7d116669772b552a1e5d6ee9c2cdb74ec69daf6856b4ab8accf9e72`.
Originaldatenbank unverändert; temporärer Restorecontainer entfernt.
Keine Wiederherstellung privater Artefaktdateien, PITR oder produktiver RPO/RTO
mit diesem Lauf nachgewiesen.

## Begrenzung

Dieser Bericht qualifiziert eine synthetische lokale Entwicklungsumgebung.
Offene P1-Funktionen und Organisationsentscheidungen stehen vollständig im
Implementierungsstatus und in der Anforderungsmatrix. Hosted CI, reale
Authentifizierung, externer Mailversand und Produktionsbetrieb sind getrennte
Nachweise; sie werden durch grüne lokale Tests nicht ersetzt.

## Ergänzender Integrationsstand

Der spätere Lauf `.checks/integration-next.log` umfasst **222 Assertions** ohne
Fehler, Warnungen oder Skips: Services 59, weitere Verträge 6, Jobs 17,
Qualitativ 30, Kommunikation 38, Authentifizierungsadapter 19, Reporting 42 und
Protokollamendments 11. Der separate Offline-Lauf umfasst 72 Assertions; 31
PostgreSQL-Testfälle sind dort erwartungsgemäß übersprungen und werden separat
in der realen Integration ausgeführt. Spätere gezielte Regressionen können die
Zahlen erhöhen; ihre eigenen Logs bleiben maßgeblich.

Der zweirundige Servicepfad wurde mit dem Quarto-Export erneut erfolgreich
vollständig durchlaufen. Die leere Datenbank wurde mit **sieben** Migrationen,
identischer Wiederholung und anschließendem Servicefixture geprüft.

Der kombinierte Restorelauf
`.checks/restore-20260925T095552Z-c07glxtv/result.json` bestätigt 45 Tabellen,
79 Snapshots, sieben Migrationen und einen ausgewählten Studienexport. Die
Artefaktbytes wurden gesichert, erneut hergestellt, gegen den registrierten
Manifesthash geprüft und offline reproduziert. Dump-SHA256:
`eaab005f8727ba552e2f651128269687aefd2a09d9961992324941a7f3d308d1`.
Nicht enthalten: andere Studienartefakte, OIDC-/SMTP-Konfiguration, produktive
Rollen, PITR und zugesicherte RPO/RTO.

`scripts/load-check.R` startete 30 unabhängige Runtime-Verbindungen über eine
Bereitschaftsbarriere. Alle 360 bestätigten Antworten wurden über neue
Verbindungen vollständig mit Revision 1 und dem erwarteten Wert 7 gelesen.
Lokales Service-/Datenbank-p95: **0,083 Sekunden**, Maximum 0,092 Sekunden.
Diese Messung enthält keine Browser-, Internet- oder TLS-Latenz und ist keine
Produktivkapazitätszusage. Nachweis: `.checks/load-check.json`.

## Abschließender lokaler Prüfstand

Der folgende Stand ersetzt die früheren Zwischenstandsangaben für den jetzigen
Quelltext. `scripts/integration.R` lief frisch gegen alle elf Migrationen:

| Testdatei | Assertions |
|---|---:|
| services | 59 |
| service-contracts | 6 |
| jobs | 17 |
| qualitative | 30 |
| communications | 38 |
| authentication | 21 |
| reporting | 45 |
| protocols | 13 |
| panel-import | 56 |
| participation | 27 |
| invitations | 26 |
| **PostgreSQL gesamt** | **338** |

Keine Fehler, Warnungen oder Skips in diesem Lauf. Separat: **77 Offlineassertions**
mit 47 ausdrücklich separaten DB-Skips sowie **115 App-Assertions**. Zahlen aus
unterschiedlichen Läufen nicht als unabhängige Gesamtabdeckung addieren.
Beide abschließenden `R CMD check --as-cran --no-manual` liefern **Status OK**,
mit gebauten Vignetten und deaktivierter Incomingprüfung. Kern-/App-R-Quellen,
Tests, NAMESPACE und SQL im gebauten Archiv wurden gegen die aktuellen Dateien
verglichen. DESCRIPTION erhält die üblichen R-Buildmetadaten.

Die unabhängige Durchsicht fand und korrigierte drei tatsächliche Fehler:
CSV-Terminatoren, nicht versionsgenaue Herkunftsfilter und den Sperrzyklus eines
laufenden Saves gegen Rückzug. Die kontrollierten Rennen prüfen beide Reihenfolgen.
Zusätzlich werden Amendments, die eine aktive Panelgruppe entfernen, früh
abgewiesen; die Rundenvorbereitung prüft die Gruppen nochmals.

Reale Browsernachweise: unabhängige redaktionelle Freigabe, genaue
Kampagnenfreigabe im Sink, Protokolländerung ohne Änderung alter Runden,
Dublettenblockierung und Zwei-Zeilen-Panelimport ohne automatische Kontobindung.
Ein Netzabbruch lässt Änderung 8 unbestätigt; Reload stellt gespeicherten Wert 7,
Revision 1 wieder her. Tastaturpfad bis zur Abgabe geprüft. Die Rechte-Navigation
enthält für die Studienleitung fünf Links und keinen Panellink; Wechsel erhält
Entwürfe. Bei 390px Viewport beträgt die Dokumentbreite 390px. Keine vollständige
Assistenztechnik-/Cross-Browserabnahme abgeleitet.

Die reale Authentifizierung mit Keycloak und OAuth2 Proxy besteht zwölf Prüfungen
im **direkten Shiny-Backendpfad**, einschließlich Header-/Cookiefälschung,
unberechtigtem Direktzugriff, isolierter Sitzung und aktueller Kontorevokation.
Stock Shiny Server OSS wurde separat real geprüft und **nicht qualifiziert**:
Gatewaylogin gelingt, Identitätsheader fehlen in R, Sitzung wird abgewiesen.
Nachweise `.local/auth/qualification.json` und `.local/auth/oss-negative.json`
enthalten keine Credentials. Einladungsannahme ist im Kern einschließlich zweier
konkurrierender Prozesse geprüft, nicht als Browseroberfläche.

Erneuter zweirundiger Servicepfad einschließlich Quarto-Export und Offlineanalyse:
bestanden. Abschließender kombinierter Restore:
`.checks/restore-20260925T101504Z-f1eirvq3/result.json`, **53 Tabellen, 119 Snapshots,
elf Migrationen und ein ausgewählter Studienexport**. Dump-SHA256:
`92947e697ba33f446e144e9128b56b26f04b97461caf1bc690f1f3d64722ed72`.
Der Umfang bleibt synthetisch; ausgeschlossene Betriebsbestandteile stehen im Receipt.

Hosted CI für den vorherigen Commit `17bb80b` war erfolgreich:
https://github.com/CTTIR/delphyR/actions/runs/36120649336.
Der neue Integrationscommit benötigt einen eigenen Hosted-CI-Nachweis.
