# Local development evidence: 2026-09-25

This is a chronological record of **historical measured checkpoints**, not an
assertion that every later source change has rerun every check. The subsequent
English-documentation/en/fr/de-interface change has its own focused evidence below;
use current logs and commit-specific CI for final acceptance.

Environment: R 4.6.1 on Linux; PostgreSQL 17, image
`sha256:67f41722b7a8cbdb868a44a4995c846eddfdc2973bccb291ce937dce88ad5675`.
Package versions are recorded in `renv.lock`. All inputs are synthetic.

## Commands used

```sh
DELPHYR_TEST_DB=true Rscript scripts/integration.R
Rscript scripts/demo-e2e.R
Rscript scripts/migration-empty.R
Rscript scripts/check-packages.R
bash scripts/restore-check.sh
```

## Initial checkpoint

- Integration: 59 + 6 + 17 + 30 + 38 = **150 assertions**, no errors/warnings.
- Pure core contracts: **63 assertions**, no errors/warnings.
- Shiny contracts: **40 assertions**, including failed save, dirty form, exact
  revision-set submission, feedback approval and timezone display. The test log
  for the actual package version is authoritative.
- Two-round service path: 30 synthetic people in two groups, twelve bilingual
  items, 720 confirmed responses, 60 submissions, personal previous responses,
  feedback, final decisions, private export and identical offline analysis.
- Both package checks: **Status OK** with `--as-cran --no-manual`, incoming checks
  disabled. No CRAN submission or acceptance claimed. Ordinary package checks
  skip database tests; separate explicit PostgreSQL runs provide their evidence.

Linux Chromium verified preserved unsaved values on language change, confirmation
only after a service response, explicit submission and receipt, and a 390px
viewport without horizontal overflow. The actual `delphyr_runtime` panel path
also recorded consent, value 7, revision 1, `submitted` and one durable receipt,
independently re-read outside the browser. Reproduction and limits are in
`packages/delphyrApp/inst/qa/`.

Initial screenshots used injected services; they are not database, accessibility,
cross-browser or complete management acceptance. An actual management startup was
also exercised, while the full management workflow remained open.

Initial restore receipt:
`.checks/restore-20260925T093049Z-4hgoqegd/result.json`: 44 tables, 32 snapshots,
six migration checksums, row fingerprints and schema matched a fresh isolated
instance. Dump SHA-256:
`d4e37589c7d116669772b552a1e5d6ee9c2cdb74ec69daf6856b4ab8accf9e72`.
Source database unchanged; temporary container removed. This run did not prove
private artifact-byte recovery, PITR or production RPO/RTO.

## Subsequent integration checkpoint

`.checks/integration-next.log` recorded **222 assertions**, without errors,
warnings or skips: services 59, further contracts 6, jobs 17, qualitative 30,
communications 38, authentication adapter 19, reporting 42, protocol amendments 11.
The separate offline run had 72 assertions and 31 intentionally skipped database
test cases, exercised separately in integration. Later regressions may increase
counts; their individual logs remain authoritative.

The complete two-round path reran successfully with Quarto export. An empty
database passed **seven** migrations, identical retry and a service fixture.

Combined restore `.checks/restore-20260925T095552Z-c07glxtv/result.json` verified
45 tables, 79 snapshots, seven migrations and one selected study export. Artifact
bytes were backed up, restored, checked against the registered manifest hash and
reproduced offline. Dump SHA-256:
`eaab005f8727ba552e2f651128269687aefd2a09d9961992324941a7f3d308d1`.
Other study artifacts, OIDC/SMTP configuration, production roles, PITR and promised
RPO/RTO were excluded.

`scripts/load-check.R` launched 30 independent runtime connections behind a
readiness barrier. Fresh connections re-read all 360 confirmed responses with
revision 1 and expected value 7. Local service/database p95 was **0.083 seconds**,
maximum 0.092 seconds. This excludes browser, internet and TLS latency and is not
a production capacity promise. Receipt: `.checks/load-check.json`.

## Verified checkpoint before the language expansion

A fresh `scripts/integration.R` run against eleven migrations superseded the earlier
intermediate integration counts for that source checkpoint:

| Test file | Assertions |
|---|---:|
| services | 59 |
| service-contracts | 6 |
| jobs | 17 |
| qualitative | 30 |
| communications | 38 |
| authentication | 21 |
| reporting | 45 |
| protocols | 13 |
| panel-import | 56 |
| participation | 27 |
| invitations | 26 |
| **PostgreSQL total** | **338** |

No errors, warnings or skips. Separately: **77 offline assertions** with 47 explicit
database skips, and **115 app assertions**. Do not add counts from separate runs
as independent total coverage. Both `R CMD check --as-cran --no-manual` runs returned
**Status OK**, with built vignettes and incoming checks disabled. Core/app sources,
tests, NAMESPACE and SQL inside built archives were compared with source files;
DESCRIPTION included normal R build metadata.

Independent review found and fixed three actual defects: CSV terminators,
non-version-specific provenance filtering and a save/withdrawal lock cycle.
Controlled races test both orderings. Amendments removing an occupied panel group
are rejected, and round preparation rechecks groups.

Actual browser evidence covered independent editorial approval, exact local-sink
campaign approval, protocol changes preserving old rounds, duplicate blocking and
two-row panel import without automatic account binding. Network loss left change
8 unconfirmed; reload restored saved value 7, revision 1. The keyboard submission
path passed. Manager navigation had five links and no panel link; switching
preserved drafts. A 390px viewport had 390px document width. No complete
assistive-technology or cross-browser acceptance was inferred.

Actual Keycloak/OAuth2 Proxy authentication passed twelve checks through the
**direct Shiny backend**, including header/cookie forgery, unauthorized direct
access, independent sessions and current account revocation. Stock Shiny Server
OSS was separately exercised and **not qualified**: gateway login worked, R lacked
identity headers and the session was rejected. Sanitized evidence:
`.local/auth/qualification.json` and `.local/auth/oss-negative.json`. Invitation
acceptance was tested in core, including two competing processes, not in browser UI.

The two-round path, Quarto export and offline analysis passed again. Combined
restore `.checks/restore-20260925T101504Z-f1eirvq3/result.json` verified **53 tables,
119 snapshots, eleven migrations and one selected study export**. Dump SHA-256:
`92947e697ba33f446e144e9128b56b26f04b97461caf1bc690f1f3d64722ed72`.
Scope remained synthetic; exclusions are listed in the receipt.

Recorded hosted CI for commit `17bb80b` passed:
https://github.com/CTTIR/delphyR/actions/runs/36120649336.
The integration checkpoint with runtime-role provisioning (`0f03ae6`, following
`d83a9b2`) also passed hosted CI:
https://github.com/CTTIR/delphyR/actions/runs/36123381681.
Unit/documentation checks, PostgreSQL contracts, empty migration, two-round
scenario and both package checks passed. Quarto and browser checks additionally
ran locally; local browser evidence is not hosted browser evidence.

After that push, the direct authentication image was rebuilt from its source
checkpoint. The actual Keycloak/proxy/websocket browser run passed all twelve
checks again. The restarted host demo used the existing database and showed an
active websocket, five rights-dependent links, no rendered errors and no overflow
at 390px. The separate synthetic worker restarted. Authentication fixture and host
demo are separate evidence scopes, especially for artifact storage and workers.

## Language expansion: focused core evidence

The new `test-languages.R` passed **12 assertions**, with no errors, warnings or
skips, against actual PostgreSQL after migration `012_content_languages.sql`.
It checks en/fr/de protocol acceptance, unsupported-language rejection, French
instrument text, French approved consent, French campaign persistence, French
contact import, and invalid scalar/vector locale inputs. The integration script
includes this test file. This focused result does not replace full package,
browser, restore or hosted-CI reruns for the language update.

English public documentation and en/fr/de interface policy supersede earlier
German/bilingual scope. A round still references one approved consent version;
interface switching does not substitute, translate or reapprove that content.

## Overall boundary

These results qualify specific synthetic local development paths. Remaining P1
functions and organizational decisions are in the implementation status and
requirements matrix. Hosted CI, institutional authentication, external messages,
scientific/clinical validation and production operation are separate gates.

## English documentation and trilingual re-audit

Current local runs passed: 81 offline assertions (48 explicit database skips),
350 PostgreSQL assertions with no failures/warnings/skips, 145 Shiny assertions,
and both package checks with `Status: OK`. The package checks built and rebuilt
both English vignettes; separate rendered HTML was inspected against CTTIR style.
Twelve migrations passed an empty-database and idempotent retry check. The two-round
scenario passed with 30 participants, 12 trilingual items and 720 committed responses.

`browser-languages.R` passed against a fresh PostgreSQL fixture through English,
French and German switching, retaining a pending rating and approved consent,
then saving and submitting in French. An independent read as `delphyr_runtime`
verified accepted consent, value 7, revision 1, submitted enrollment and a durable
receipt. Manager language switching preserved the typed rationale and localized
headings/upload controls without Shiny output errors. Desktop/mobile overflow
checks passed. Approved study content remains explicitly authored.

Local run logs: `.local/reaudit-{core-tests,integration,app-tests,package-checks,
e2e,migration,browser-languages,manager-languages}.log`; built checks remain under
`.checks/`. Hosted CI is a separate post-push gate.
